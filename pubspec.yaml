name: myapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

# Updated to support Dart beta versions
environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Cupertino Icons for iOS style icons
  cupertino_icons: ^1.0.8

  # HTTP requests
  http: ^1.3.0

  # Audio playback
  just_audio: ^0.9.36

  # State management
  provider: ^6.1.1

  # Local storage
  shared_preferences: ^2.2.2

  # File system access
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
